#include <iostream>
#include <cmath>
using namespace std;
int main ()
{
 float a,b;
 cout<< "enter base and exponent respectively"<< endl;
 cin >> a >> b;
 cout<< "the value of base to the power exponent=" << pow(a,b);
 return 0;
}
